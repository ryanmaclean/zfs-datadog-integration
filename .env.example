# VM Storage Configuration
# Copy this file to .env and customize for your environment

# Storage location for VM images and ISOs
# Default: Local storage in ~/.vfkit
# Dev/Remote: Use remote ZFS server to save local disk space
VM_STORAGE_DIR="$HOME/.vfkit"

# Optional: Remote storage via NFS/SSHFS mount
# Example for NFS mount: /Volumes/tank3
# Example for SSHFS: /mnt/remote-zfs
# Dev environment: /Volumes/tank3/vms (i9-zfs-pop.local)
# VM_STORAGE_DIR="/Volumes/tank3/vms"

# Download cache location for ISOs and cloud images
# Set to remote storage to avoid filling local disk
DOWNLOAD_CACHE_DIR="$HOME/.vfkit/downloads"
# Dev environment: /Volumes/tank3/iso-cache
# DOWNLOAD_CACHE_DIR="/Volumes/tank3/iso-cache"

# Remote ZFS server configuration (optional)
# Only needed if using remote storage
# REMOTE_ZFS_SERVER="i9-zfs-pop.local"
# REMOTE_ZFS_PATH="/tank3"
# REMOTE_ZFS_MOUNT="/Volumes/tank3"

# Disk space thresholds (GB)
MIN_FREE_SPACE_GB=20
WARN_FREE_SPACE_GB=50

# Auto-mount remote storage (optional)
# Set to "true" to automatically mount remote ZFS via NFS
AUTO_MOUNT_REMOTE="false"
